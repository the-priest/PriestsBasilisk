
name: Build Windows EXE

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-python
            mingw-w64-x86_64-python-pip
            mingw-w64-x86_64-python-gobject
            mingw-w64-x86_64-python-cairo
            mingw-w64-x86_64-gtk4
            mingw-w64-x86_64-libadwaita
            mingw-w64-x86_64-gobject-introspection
            mingw-w64-x86_64-gsettings-desktop-schemas

      - name: Install PyInstaller inside MSYS2
        shell: msys2 {0}
        run: |
          python -m pip install pyinstaller

      - name: Build Executable
        shell: msys2 {0}
        run: |
          pyinstaller --onefile \
            --collect-all gi \
            --collect-all cairo \
            --add-data "/mingw64/lib/girepository-1.0;gi_typelibs" \
            --add-data "/mingw64/share/glib-2.0/schemas;share/glib-2.0/schemas" \
            --add-data "assets;assets" \
            basilisk.py

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: basilisk-windows-exe
          path: dist/basilisk.exe
